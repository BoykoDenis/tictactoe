import tensorflow as tf
from tensorflow import layers
from tensorflow.contrib.keras.models import Sequential
model = Sequential()
#def model
def NeuralModel(model, count, ):
	model.add(Dense(361, input_shape = (19, 19))
	model.add(Dense(361, input_shape = (19, 19))
	model.Dropout(0.25)
	model.add(Dense(361, input_shape = (19, 19), activation = "AdamOptimizer"))
	model.fit()
